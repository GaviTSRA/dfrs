save NPCS = `npcs`;

fn getNPC(id: string) {
    local npc;
    v.getDictValue(npc, NPCS, id);
}

fn spawnNPCs() {
    line id;
    line npc;
    line name;
    line skin;
    line loc;

    //for.entry(id, npc, NPCS) {
        v.getListValue(id, npc, 1);
        v.getListValue(name, npc, 2);
        v.getListValue(skin, npc, 3);
        v.getListValue(loc, npc, 4);

        g.spawnArmorStand(loc);
        e.playerDisguise(name, skin);
        e.setCustomTag('npc', 1);
        e.setCustomTag('id', id);
        e.setAI(ai='None');
        e.setInvulnerable();
        e.setSilenced();
    //}
}