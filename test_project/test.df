fn setLocationToNearestLayer(loc_in: location) {
    line y;
    local loc;

    v.getCoord(y, loc_in, coordinate="Y");
    v.equal(loc, Location(0.5, 46.5, 0.5));
    ifv inRange(y, 40, 51, locationHandling="Exact") {
        v.setCoord(loc, loc_in, 49.5, coordinate="Y");
    }
}